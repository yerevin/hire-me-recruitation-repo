// create file config.tsx with below content and set proper apiUrl

export const config = {
  version: "1.0.0",
  apiUrl: "http://localhost:8080" // replace localhost with your local ipv4 to connect local api from emulator
};
